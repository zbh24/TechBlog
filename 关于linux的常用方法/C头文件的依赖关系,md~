#关于头文件

1.头文件主要存放的是
- 函数声明
- 全局变量
- 结构体的定义和声明

2.之所以，在每个头文件前面加一个条件包含，是为里防止变量重新声明，这样是会报错的。函数声明无所谓。

3.关于管理
一方面希望每个文件能完成它要的功能，一方面也想尽少地维护头文件。那我们进行了折衷处理。
中等规模一个头文件就好。

4.如果每个头文件都使用了条件依赖，那我们肯定都能包含进来，而不必头文件的依赖关系。
